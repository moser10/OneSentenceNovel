name = "onesentencenovel"
main = "worker.js"
compatibility_date = "2024-09-23"

[assets]
directory = "./public"
binding = "ASSETS"

# D1: Cloudflare Dashboard → Workers & Pages → 你的 Worker → Settings → Bindings
# Add D1 binding, variable name must be "DB"
